import {
  setupSingleTextureScene,
  setupTriangleScene,
  setupFireworksScene,
} from '@/playground';

console.log('Hello. Setting up playground...');

setupFireworksScene();
setupTriangleScene();
setupSingleTextureScene();
